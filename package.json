{
    "name": "playwright-bdd-full-hybrid",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "tsc",
        "report:html": "node ./src/helper/generate-report.cjs",
        "test:main": "rimraf reports && npm run build && cucumber-js features/createorder.feature features/loginlogin_Shop_Neg_edge.feature --import dist/**/*.js --format progress --format json:reports/cucumber-report.json & npm run report:html",
        "test:bugs": "rimraf reports && npm run build && cucumber-js features/test_bugs_site.feature --import dist/**/*.js --format progress --format json:reports/cucumber-report.json & npm run report:html",
        "test:api": "rimraf reports && npm run build && cucumber-js features/api/brands.feature --import dist/**/*.js --format progress --format json:reports/cucumber-report.json & npm run report:html"


    },
    "devDependencies": {
        "@cucumber/cucumber": "^12.3.0",
        "@types/node": "^24.10.2",
        "cucumber-html-reporter": "^7.2.0",
        "playwright": "^1.57.0",
        "rimraf": "^6.1.2",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "@playwright/test": "^1.57.0"
    }
}
